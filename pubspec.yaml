name: metar
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  units: ^0.0.7

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
  tuple: ^1.0.3

